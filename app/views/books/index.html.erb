<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Book List</title>
  </head>
  <body>
    <nav>
      <div class="nav-wrapper">
        <a href="https://kidslibrary.herokuapp.com#" class="brand-logo">Iron Library</a>
        <ul id="nav-mobile" class="right hide-on-med-and-down">
          <li><%= link_to 'Book List', books_path %></li>
          <li><%= link_to 'New Book', new_book_path %></li>
          <li><%= link_to 'Author List', authors_path %></li>
          <li><%= link_to 'New Author', new_author_path %></li>
        </ul>
      </div>
    </nav>
    <div class="container">
      <div class="row">

        <% @books.each do |book| %>

        <div class="col s12 s6 l4">
          <div class="demo-card-square mdl-card mdl-shadow--2dp" style="max-width: 100%;">
            <div class="mdl-card__title mdl-card--expand" style="background: url(<%= book.photo_url %>); background-repeat: no-repeat;"></div>
            <div class="mdl-card__supporting-text">
              <%= link_to book.title, book_path(id: book.id), style: "text-decoration: none; color: #666666; font-size:20px;"%>
            </div>
            <div class="mdl-card__actions mdl-card--border">
              <strong style="padding-right: 10px;">
                <%= number_to_currency(book.price) %></strong>
              <%= link_to book.author.full_name, book_path(id: book.id), class: "waves-effect waves-light btn" %>
            </div>
          </div>
        </div>
        <% end %>

      </div>

    </div>
  </body>
</html>
